
import  isEmpty  from '../validation/is-empty';


const initialState = {
    games: [],
    game: {}
}

export default function(state = initialState, action) {
    switch(action.type) {

       
            
        default: 
        return state;
    }
}